extends layout

block content
  #selectTrip
    h1 Select a Trip
    
    #trip
    .btn.btn-green.btn-select-trip Ok
    
  #selectFriends.hide
    h1 Split it up
    
    input.friendEmail(type="text" placeholder="Enter an email or phone number")
      
    .mileageCostContainer
      label Cost Per Mile
      .mileageRate
      input.mileageRateSlider
      
    label Split Between  
    .splitList
      

block scripts
  script(type="text/template" id="tripTemplate")
    .mapContainer
      .prevTrip &#9664;
      #map.map
      .nextTrip &#9654;
    .tripData
      .topData
        .from
          label From
          .fromAddress {{StartAddressAddress}}
          .fromCity {{StartAddressCityState}}
        .to
          label To
          .toAddress {{EndAddressAddress}}
          .toCity {{EndAddressCityState}}
      .bottomData
        .cost
          label Cost
          .costData
        .when
          label When
          .whenDate {{StartDate}}
          .whenTime {{StartTimeFormatted}}
  
  script(src="/javascripts/main.js")
